apiVersion: v1
kind: Service
metadata:
  name: {{ include "nmapshot.fullname" . }}
  labels:
    {{- include "nmapshot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nmapshot.selectorLabels" . | nindent 4 }}
